# frozen_string_literal: true

source 'https://rubygems.org'

ruby '~> 2.5' # sync with .ruby-version

gem 'nokogiri'
gem 'sinatra'
gem 'puma'
gem 'redis'
gem 'redis-namespace'

group :development, :test do
  gem 'shotgun'
  gem 'webmock'
  gem 'rspec'
  gem 'byebug'
end
